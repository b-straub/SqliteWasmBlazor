diff --git a/node_modules/@sqlite.org/sqlite-wasm/sqlite-wasm/jswasm/sqlite3-bundler-friendly.mjs b/node_modules/@sqlite.org/sqlite-wasm/sqlite-wasm/jswasm/sqlite3-bundler-friendly.mjs
index ac82b14..5ae5efd 100644
--- a/node_modules/@sqlite.org/sqlite-wasm/sqlite-wasm/jswasm/sqlite3-bundler-friendly.mjs
+++ b/node_modules/@sqlite.org/sqlite-wasm/sqlite-wasm/jswasm/sqlite3-bundler-friendly.mjs
@@ -13577,6 +13577,25 @@ var sqlite3InitModule = (() => {
             return !!sah;
           }
 
+          renameFile(oldPath, newPath) {
+            const sah = this.#mapFilenameToSAH.get(oldPath);
+            if (!sah) {
+              toss('File not found:', oldPath);
+            }
+
+            // Read current flags from SAH header
+            sah.read(this.#apBody, { at: 0 });
+            const flags = this.#dvBody.getUint32(HEADER_OFFSET_FLAGS);
+
+            // Remove old path mapping
+            this.#mapFilenameToSAH.delete(oldPath);
+
+            // Update SAH header with new path (physical file stays the same)
+            this.setAssociatedPath(sah, newPath, flags);
+
+            return true;
+          }
+
           storeErr(e, code) {
             if (e) {
               e.sqlite3Rc = code || capi.SQLITE_IOERR;
@@ -13815,6 +13834,10 @@ var sqlite3InitModule = (() => {
             return this.#p.deletePath(filename);
           }
 
+          renameFile(oldPath, newPath) {
+            return this.#p.renameFile(oldPath, newPath);
+          }
+
           async removeVfs() {
             return this.#p.removeVfs();
           }
