@page "/"
@using SqliteWasmBlazor.Demo.Services
@inject DBInitializationService DBInitializationService

<PageTitle>SqliteWasmBlazor</PageTitle>

<MudStack>
    <MudText Typo="Typo.h5">SqliteWasmBlazor</MudText>
    <MudText>The first known solution providing true filesystem-backed SQLite database with full EF Core support for
        Blazor WebAssembly.
    </MudText>

    @if (DBInitializationService.ErrorMessage is not null)
    {
        <MudText Color="Color.Error">@DBInitializationService.ErrorMessage</MudText>
    }
</MudStack>
